# Frontend Variables (apps/web)
# ==========================================

# 1. Public URL (Required for redirects)
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:4000

# 2. Google Authentication (Required for Google Login)
# Get this from Google Cloud Console -> APIs & Services -> Credentials
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your_google_client_id_here.apps.googleusercontent.com

# 3. GitHub Authentication (Required for GitHub Login)
# Get this from GitHub Developer Settings -> OAuth Apps
NEXT_PUBLIC_GITHUB_CLIENT_ID=your_github_client_id_here

# 4. Dashboard Analytics (Optional)
NEXT_PUBLIC_DEFAULT_ORG_SLUG=genesis

# 5. Stripe (Optional for Billing)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
